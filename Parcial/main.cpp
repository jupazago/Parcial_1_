#include <iostream>

using namespace std;

int main()
{
    bool menus[3]={false,false,false};
    while (menus[0]==false) {
        //Vamos a agregar los datos de entrada de ambos cañones
    }
    return 0;
}
